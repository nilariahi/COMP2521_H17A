typedef struct graph {
	// TODO define a Graph structure to work with
}* Graph;

struct edge {
	// TODO define the edge structure
};

// Check whether a path (struct edge array) is an Euler path
bool isEulerPath(Graph g, struct edge e[], int nE) {
	// TODO
}
